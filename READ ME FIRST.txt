Upload BOTH files to the repo root, replacing what is there:

  app.py
  theme_css.py

Then reboot and hard-refresh (Cmd-Shift-R).

WHAT CHANGED - the sidebar now actually matches the mock
--------------------------------------------------------
app.py:
  * Brand block: the dark "TPC" circle with "Command Centre" and
    "The Tea Party Cafe" beneath it, replacing "### Cafe Manager".
  * User chip: a sand pill with the staff member's initials and
    "Priya . Owner", replacing the uppercase "LOGGED IN AS" caption.
  * The nav is now split into two labelled groups - "Daily running"
    and "Owner tools" - as in the mock, instead of one long list.

theme_css.py:
  * Nav rows tightened: smaller text (0.845rem), less padding, and
    the Material icons shrunk and muted so the label leads the row
    rather than the icon.
  * Active page icon lightened so it reads on the dark pill.

WHAT STILL DIFFERS FROM THE MOCK
--------------------------------
The mock uses small dots instead of icons, and shows counts like
"6 open" beside Tasks. Both are doable - say the word. The bigger
remaining gap is the Tasks page: seven full-width day cards stacked
vertically, where the mock has a compact grid. That is a layout
change in app.py, not styling.
